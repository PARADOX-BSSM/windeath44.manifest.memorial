apiVersion: v1
kind: Service
metadata:
  name: {{ include "memorial.fullname" . }}
  labels:
    {{- include "memorial.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  {{- range .Values.service.ports }}
    - name: {{ .name }}
      port: {{ .port }}
      targetPort: {{ .targetPort }}
      protocol: TCP
  {{- end }}
  selector:
    {{- include "memorial.selectorLabels" . | nindent 4 }}
