apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "memorial.fullname" . }}-test-connection"
  labels:
    {{- include "memorial.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "memorial.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
